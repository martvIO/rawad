# npm 10/11 on Windows crashes with "Cannot read properties of undefined (reading
# 'stdin')" when process.stdin is null (VS Code terminal, non-TTY contexts).
# foreground-scripts makes npm run child scripts with the parent's own stdio
# instead of piping, which prevents the null-stdin crash.
foreground-scripts=true
