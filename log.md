# Wiki Log

Chronological, append-only record of everything that's happened in this wiki.

**Format:**
```
## [YYYY-MM-DD HH:MM] <type> | <title>
<optional detail line>
```

**Types:** `session`, `ingest`, `query`, `lint`, `rebuild`

**Quick access:** `grep "^## \[" log.md | tail -5` gives you the last 5 entries.

---

## [2026-05-26 17:35] ingest | Dawa project docs (initial wiki build)
Touched: Dawa, User Roles, REST API Architecture, Authentication, Polling and Realtime, Data Storage Model, Security Model, Digital Invitations, Optimistic UI Pattern, Inline Styling Convention, API Contracts, Architecture Decisions, Known Bugs, Tasks Backlog, AI Engineering Rules

## [2026-05-28] session | Full Playwright QA audit of live production app
Touched: none (new report file qa-audit-2026-05-28.md written to repo root; project memory saved)

## [2026-05-28 07:03] session | Add SessionStart hook for wiki-brain
Touched: none (config/hook setup, not a wiki page change)

## [2026-05-28 16:05] session | Self-serve digital design editor + approval gate
Touched: Digital Invitations, API Contracts, Architecture Decisions, Tasks Backlog

## [2026-05-28 16:45] session | Implement luxury digital-invitation design + full groom customization
Touched: Digital Invitations. Rebuilt DigitalInvitationView as the multi-section editorial microsite from the dawa-design-system handoff bundle (envelope → hero → story timeline → gallery+lightbox → details → venue+map+hotels → countdown → enhanced RSVP → guestbook → footer → dock), scoped under .dawa-inv, theme-token driven. Expanded DigitalDesignEditor so the groom edits every field (new src/data/digitalInviteDefaults.js + "fill sample" button + per-section toggles). Backend: new fields/validation in digital.ts sanitizeMediaSettings + DESIGN_FIELDS + design-list; RSVP headcount/meal/song + full designSnapshot in invites.ts. Public design browser-verified via /d demo route; editor build-verified (auth-gated).

## [2026-05-28 17:10] session | Add companions count to both invite types
Touched: Digital Invitations. Unified `companions` field (people attending besides the invited guest, 0–20, default 0) on digital + physical RSVP. Reframed the just-added digital `headcount`→`companions` (renamed toggle rsvpHeadcountEnabled→rsvpCompanionsEnabled). Backend: invites.ts (digital + physical submit/patch/mirror), confirmations.ts (open form + auto-attach), database.rules.json (guestsByGroom + confirmations validate, both had $other:false). Frontend: digital RSVP stepper (default 0, functional updaters for rapid taps), new shared src/components/CompanionsStepper.jsx on ConfirmationForm + InviteForm, conf_form_companions i18n key. Groom display: per-guest +N badges (DigitalGuests, GroomGuests) + "expected attendees" totals (DigitalDashboard, usePortalState→GroomDashboard) + admin AdminConfirmationsTab. Digital companions stepper browser-verified; physical write path needs RTDB rules deployed/emulated to accept the new field.

## [2026-05-28 21:10] session | Western digits across website + database
Touched: Digit Normalization (new). Standardized on Western ASCII digits everywhere. New src/utils/digits.js (toWesternDigits + westernizeDeep, preserves password keys); apiClient scrubs every request body so DB only stores Western digits; PhoneInput normalizes typed Arabic-Indic digits (its \D strip otherwise drops them). Added numberingSystem:"latn" to 9 Arabic-locale toLocale* date/time sites (keeps Arabic month names, Western digits — verified via Node ICU). Converted literal Arabic-Indic digits in sample/default content (digitalInviteDefaults.js, DigitalInvitationPage demo, DigitalDesignEditor placeholders). New scripts/migrate-arabic-digits.cjs (npm run migrate:digits) walks RTDB+Firestore, dry-run+emulator by default, --commit/--prod to write; NOT yet run against prod (user runs it). Unit tests in src/__tests__/utils/digits.test.js (all green); build passes. Browser verify skipped — Playwright MCP browser was locked by another session.

## [2026-05-29 16:05] session | Bilingual invitation content + RSVP/headcount/phone + groom-send lockout + RTL number alignment
Touched: Digital Invitations. Six-feature build from the translated/enhanced prompt. (1) Prominent guest name in the digital hero greeting (`.dawa-inv-greet strong` → block, clamp(30–46px), weight 900, gradient). (2) **Bilingual groom content**: new `src/utils/localize.js` (localize/localizeItems/localizeList/hasContent); `DigitalInvitationView` renders every text field via localize; new guest AR/HE `LangToggle` wired to app lang/setLang; `DigitalDesignEditor` got an editLang tab + leaf/setLeaf so each text input/array-leaf edits one language; backend `digital.ts` `sanitizeMediaSettings` accepts `{ar,he}` via `clampLocalized` (types widened to `Localized`); demo route now bilingual. (3) **Total headcount** incl. guest (min 1) — UI tracks partySize, still stores `companions = partySize−1` (no backend/rules change); `CompanionsStepper` + ConfirmationForm + InviteForm + digital RSVP; `conf_form_companions` reworded. (4) **Required editable phone** on digital RSVP — `submittedPhone` added to `/invites/digital/submit` (normalisePhone-validated, stored as guest phone). (5) **Groom WhatsApp send removed** — deleted GroomGuests send button; admin-only. (6) **RTL number alignment** — new `src/components/Num.jsx` (`<bdi dir=ltr>` isolate), 45 wraps across guest pages + portals (sub-agent sweep). Verified: frontend + functions builds pass; unit tests 416 pass (+8 new localize tests; 2 pre-existing apiClient/buildApiUrl env failures unrelated); Playwright smoke of the demo invitation (name 46px, AR↔HE switches all content, total-headcount label + min 1, required-phone block) and the confirmation form. NOT yet committed/deployed.

## [2026-06-02 02:30] session | Faster digital media uploads — progress bar + instant design preview
Touched: Digital Invitations / Uploads. Full resolution kept everywhere per user (compression declined). apiClient.upload() now uses XMLHttpRequest when opts.onProgress is passed (xhr.upload.onprogress → 0..1), preserving the auth/401-retry/timeout/abort/parseResponse semantics; fetch stays the default for callers without onProgress (proof uploads + unit tests untouched). DigitalDesignEditor: optimistic-insert the uploaded item into doc.media/heroMedia so the image appears ~8ms after the upload response instead of waiting up to the 15s subscribeDesign poll, plus % on the gallery/hero buttons. DigitalDashboard + DigitalPhotographer: aggregate per-file fractions into one % on the add tile / uploading indicator. Verified live on dawa.to via Playwright + injected groom session: dashboard upload 200 with progress 0→17→35→52→70→87→100% (before: none); design upload 200, img appears 8ms after response, no page errors; Vite build + 111 unit tests pass. Frontend-only — no backend change: Cloud Functions v2 already buffers the whole request body into req.rawBody (server streaming can't help), and direct-to-Storage needs bucket CORS + the Token Creator IAM role the SA key lacks. Deployed hosting; committed fc43d9c.

## [2026-06-02 03:10] session | Fix "صورك" camera — live feed + green overlay + dynamic guidance
Touched: Digital Invitations / Face page (DigitalYourPhotos.jsx). Root cause of the reported black screen: getUserMedia ran in the "requesting-camera" stage but the <video> only mounted in "liveness", so videoRef.current was null and srcObject was never set — detection then ran on an empty video and hit the 12s timeout. Fix: a render-driven bindStream() effect attaches the MediaStream the moment the <video> mounts, and the camera view now renders across requesting-camera/liveness/extracting so the element exists when the stream is ready. Added a mirrored <canvas> overlay that draws a green faceapi.draw.DrawBox + drawFaceLandmarks over the live video each detection frame (matchDimensions/resizeResults), plus state-driven guidance text (جاري تشغيل الكاميرا… → ضع وجهك داخل الإطار → التفت لليمين/لليسار الآن 🟢 → تم التحقق ✓). Verified live on dawa.to with Chrome fake camera + injected groom session (photographerPublished toggled true then restored): streamBound=true, video 640x480 playing, overlay canvas sized to the video (drawOverlay runs), guidance renders, no page/console errors. face-api.js 0.22.2 draw APIs confirmed present. Vite build passes. Frontend-only; deployed hosting; committed 1eb630e.

## [2026-06-02 03:40] session | Fix "صورك" camera restart loop
Touched: Digital Invitations / Face page (DigitalYourPhotos.jsx). Reported: tapping into the photos camera showed the live video for ~1s, then "جاري تحميل مكتبة التعرف" again, repeating. Root cause: subscribeInviteToken uses createPoller, which re-fires its callback on EVERY poll (poller.js explicitly does not dedupe), and Step-1's callback unconditionally called setStage("loading-models") each time → the whole camera flow restarted every ~3s. (Loop pre-existed but was masked by the earlier black-screen bug; once the camera actually rendered it became visible.) Fix: a routedRef guard routes the stage ONCE — after entering the camera flow, later polls only refresh groomUid and never reset the stage; the not-published branch stays unlocked so it still auto-advances when the groom publishes. Also reset cancelledRef on mount (remount safety) and stop any prior MediaStream before re-acquiring (clean retry, no double camera stream). Verified live on dawa.to (fake camera): camera view stays stable 12s with loadingReappeared=0, no console/page errors (the subsequent "انتهت مهلة الفحص" is the normal 12s liveness timeout because the fake camera has no real face). Build passes; deployed hosting; committed ebf6982.

## [2026-06-02 04:05] session | Step-by-step liveness for "صورك" — easier + mirror-robust
Touched: Digital Invitations / Face page (DigitalYourPhotos.jsx). User tested on a real phone and the head-turn liveness would not pass. Redesigned: (1) sequential one-turn-at-a-time flow instead of needing both turns at once; (2) mirror-robust direction — step 1 confirms on the guest's FIRST clear turn in either direction (raw front-camera mirroring varies per device) and records its sign, step 2 requires the opposite sign, so labels still read right-then-left and it can't fail on a wrong sign; (3) easier + more time — YAW_TRIGGER 15→13°, 2-frame debounce, STEP_TIMEOUT 20s PER step (resets after the first turn, ~40s total) replacing the single 12s budget, detector scoreThreshold 0.5→0.4; (4) clear guidance ("الخطوة 1: أدِر رأسك إلى اليمين 👉" → "✓ الخطوة 2: … إلى اليسار 👈") and indicators reordered right-first. Regression-verified live (fake camera): camera binds, detection loop runs, no restart loop, no JS errors. The turn sequence itself is device-tested by the user (automation has no real face to turn). Build passes; deployed hosting; committed 2d0aa2b.

## [2026-06-03 01:45] session | Digital RSVPs reach admin Confirmations page with headcount
Touched: Confirmations / Digital Invitations. Problem: a guest pressing "سأحضر" on the digital invitation (/d/:groom/:token) never showed on /portal/admin/confirmations — that page reads RTDB /confirmations, but POST /invites/digital/submit only patched the Firestore guest doc (digitalInvitations/{uid}/guests/{id}); the companions headcount lived where the admin never looked. Fix (backend invites.ts /digital/submit): on ATTENDING, mirror a record into /confirmations/dg_{guestId} (deterministic key → upsert) via Admin SDK with source:"digital" + companions, reusing resolveGroomUsername(); on absent, remove it (attendees-only per user). Best-effort try/catch so it never fails the RSVP. Frontend: usePortalState classificationMap overrides source:"digital" → MATCH_STATUS.GREEN/guest:null/reasons:[] (else classifyAll marks them "unknown" since digital guests aren't in the RTDB match pool); AdminConfirmationsTab now shows TOTAL persons "العدد: N أشخاص" on every card (was "+N مرافق" only when >0), a "📱 رقمي" badge for digital, and skips the empty address line for digital. New scripts/backfill-digital-confirmations.cjs (dry-run default, --apply) mirrors existing attending digital guests; applied to 4 existing RSVPs. Verified live (admin token inject + real POST): attending(3) shows in the green matched section with "العدد: 3 أشخاص" + رقمي badge, decline does not appear. No DB schema/rules change (Admin SDK write to existing /confirmations shape). functions+frontend build pass; 434 unit tests pass; deployed functions+hosting; committed 78e6022.

## [2026-06-03 02:00] session | Groom guests page shows total persons not "+N مرافق"
Touched: Digital Invitations / Guests. Trivial display fix in src/pages/portal/groom/digital/DigitalGuests.jsx (~line 379): the attending-guest badge showed "+N مرافق" (companions besides the guest), which confused the groom. Now shows the total the guest picked = companions + 1 → "👥 N أشخاص" / "👥 N אנשים". Condition relaxed from Number(companions) > 0 to companions != null so a guest who picked exactly 1 (companions 0) also shows "👥 1 أشخاص"; designs without the headcount counter (companions null) still show no badge. Mirrors the admin Confirmations page wording shipped earlier today. No tests referenced the old text. Verified live (groom token inject): the backfilled guest who chose 4 shows "👥 4 أشخاص" and no "مرافق" remains. Build + 434 unit tests pass; deployed hosting; committed 0863562.

## [2026-06-03 02:55] session | Driver: fast delivery confirm + all guests geocoded on map
Touched: Driver portal (delivery confirm + map). Issue 1 — "تأكيد التسليم" was slow / sometimes failed: the proof photo was uploaded full-resolution and the list only updated on the 15s poll. Fix: new src/utils/imageCompress.js (canvas downscale -> small JPEG dataURL, fallback to original); DriverDeliveryList + GuestMapModal compress the proof at capture; markGuestDelivered (usePortalState) is now optimistic — guest moves to "delivered" instantly, proof upload + status PATCH run in a background IIFE, revert on failure, and an optimisticDeliveredRef overlay applied in the guests subscription prevents a mid-confirm poll from flickering it back. Issue 2 — the driver map only showed guests who shared GPS (most missing). New src/utils/geocode.js geocodes the guest's written address via Nominatim restricted to countrycodes=il,ps (accurate for Arabic city names — Photon returned wrong-country pins, so dropped; never returns out-of-region), self-throttled to 1 req/s, AbortController timeout + localStorage cache (computed once). DriverMap geocodes guests lacking GPS (full address then extractCity fallback) with a HARD per-guest Promise.race cap (8s) so one slow/hanging lookup can't stall the batch (root cause of an observed "1/6" stall), a progress banner, and an "approximate location" note (new i18n map_approx_location) in GuestMapModal. The existing الكل/تم التسليم/بانتظار التسليم filters now cover every guest. Verified live with an injected driver session + temp address-only test guest: it geocodes onto the map (accurate Haifa pin), pages render with no errors. Note: live verification was flaky because repeated test queries rate-limited Nominatim from this IP — a real driver at 1 req/s is compliant; the per-guest cap + cache keep it robust. Build + 434 unit tests pass; frontend-only; deployed hosting; committed 715ff5d.

## [2026-06-03 03:15] session | Groom guests map: geocode addresses (same as driver)
Touched: Groom portal (GroomGuestsMap.jsx). Mirrored the driver-map geocoding into the groom's guests map (route /portal/groom/handwritten/map): guests lacking precise GPS are placed by geocoding their written address via the existing src/utils/geocode.js (Nominatim IL/PS, cached, self-throttled), with the hard per-guest Promise.race(8s) cap so one slow lookup can't stall the batch, a gold "جارٍ تحديد مواقع المعزومين … X/Y" progress banner, and the shared GuestMapModal approximate-location note. The existing الكل/تم التسليم/بانتظار التسليم filters now cover every guest. Verified live with an injected groom session + seeded geocode cache (Nominatim rate-limited my IP from testing): an address-only guest is geocoded onto the map, no page errors. (Test route gotcha: the map is at /portal/groom/handwritten/map, not /portal/groom/map.) Build + 434 unit tests pass; frontend-only; deployed hosting; committed 658c69a.

## [2026-06-03 13:30] session | Guest invite-link location overrides groom's preset
Touched: Invites backend (functions/src/api/routes/invites.ts). Requirement: when a guest sets a location in their invite link, it must override whatever the groom set earlier. A pin/GPS already overwrote lat/lng (buildPhysicalGuestPatch sets them on hasCoords, no guard). Gap: if the guest entered a NEW address WITHOUT a pin, area updated but the groom's stale lat/lng stayed, and the map (prefers precise coords over geocoded address) kept the old location. Fix in POST /invites/submit: after building the patch, if !hasCoords AND the submitted area differs from the stored guest area AND stale lat exists, null out lat/lng/locationSource/locationAccuracy (+ locationUpdatedAt) so the map geocodes the address the guest actually entered. Pin/GPS still overrides. Verified live (Admin SDK + real POST /api/invites/submit): A) address-only "الناصرة" submission cleared the groom's Haifa pin and updated area -> staleClearedAndAddressUpdated=true; B) a guest pin (Nazareth) overrode -> guestPinOverrides=true. functions build passes; deployed functions; committed d195bc8. NOTE: confirmation-form auto-attach (tryAutoAttach) still only sets location when the guest had none (the "first time only" caveat from the prior turn) — not changed, only the invite-link flow per the request.

## [2026-06-03 23:55] session | Groom map: driver live-location tab + SSE-through-Hosting fix
Touched: Groom map (GroomGuestsMap.jsx), live-location SSE (services/liveLocations.js, config/index.js). Added a view switcher next to "خريطة المعزومين": a new "موقع السائق" tab renders the assigned driver's LIVE location on a second LiveMap, auto-updating whenever a driver shares with this groom. The whole pipeline already existed — useGeolocation subscribes via subscribeDriversForGroom(activeGroomUid=currentUid) and exposes groomMapMarkers (kind "driver"/"you") + driversSharingWithMe through ...geo in usePortal — so the page change is pure UI: tab switcher (gold tabStyle), live count + green dot on the tab, "📍 موقعي" (requestGroomLocation) button, empty state when no driver sharing. Used a Workflow (5 agents) to map the subsystem first. CRITICAL latent bug found + fixed: the live-location SSE never worked through Firebase Hosting — Hosting buffers streamed responses so the same-origin /api/.../stream EventSource hangs (opened=false, no data). Direct Cloud Run URL streams fine (200 text/event-stream, delivers immediately; browser EventSource opens cross-origin OK via CSP connect-src + function CORS). Fix: new SSE_BASE_URL config (https://api-je74slt7ra-uc.a.run.app, VITE_SSE_BASE_URL override) used ONLY for the SSE EventSource in subscribeDriversForGroom; regular JSON stays on same-origin /api. Verified live (inject groom session + heartbeat a /liveLocationsByGroom/{groomUid}/{driverUid} record): the موقع السائق tab shows the driver marker on the map + live count, tabShowsLive=true driverMapRendered=true, no page errors. Build + 434 unit tests pass (the subscribeDriversForGroom test uses toContain so the direct-URL prefix is fine). Frontend-only; deployed hosting; committed a76479d.

## [2026-06-04 00:30] session | OG image personalised with guest name + description fix
Touched: Digital OG preview (functions/src/digitalOgImage.ts, digitalInvitePreview.ts). renderOgImage(design, guestName) now draws the per-link guest name ("إلى: {name}") under the couple, so each guest's WhatsApp preview is addressed to them; the /og/{token}.jpg handler reads guestName from inviteTokens/{token}.guestName. Date+venue combined into one line; couple stays the hero (auto-shrink helper extracted). Also fixed digitalInvitePreview og:description: brideName/groomDisplayName are bilingual {ar,he} objects so the old description was "[object Object]" — added loc() (Arabic-preferred) + formatDate() and the description is now "{couple} يتشرّفون بدعوتكم لحضور حفل زفافهم — {date}" (og:title already had the guest name). Verified live (cache-busted /og fetch + Read): image shows "إلى: روااد حجير" (Arabic shaped correctly via Amiri/@napi-rs/canvas) and "إلى: dfg dfgh" with couple + date·venue, 1200x630. functions build passes; deployed digitalOgImage+digitalInvitePreview; committed c984afa. (User also asked for a PHP/GD equivalent for reference — provided in chat, not deployed since the stack is Node/Firebase.)

## [2026-06-04 01:15] session | Digital invitation: floral flourishes + Dawa brand lockup
Touched: src/components/digital/DigitalInvitationView.jsx. User wanted the Dawa logo in every design + more beautiful (flowers). The invitation already had a "دعوة" footer wordmark + ambient petals but subtle. Added a new themed FloralFlourish SVG (central 6-petal blossom + two leafy vines + end buds, colored from theme.accent/accentMuted so it adapts across all 5 palettes): a floral crown at the top of the Hero (above eyebrow/monogram) and the same flourish in the InviteFooter above an enlarged "دعوة" brand wordmark (34->38px) with tagline "— بطاقة دعوة رقمية · صُنعت بحبّ —" (so the brand mark is clearly on every design). Made ambient petals lusher (12->18, opacity .45->.55). Pure presentational — renders the public guest page + the editor live preview, no data/backend change. Verified live (mobile viewport screenshot of /d/groom/{token}): hero floral crown + footer flourish + brand render beautifully, 18 petals, footMark present, no page errors. Build passes; deployed hosting; committed bc4f2b2.

## [2026-06-04 01:55] session | Digital invite: gold Dawa logo crown + floral-wreath monogram
Touched: src/components/digital/DigitalInvitationView.jsx. User supplied the Dawa logo (navy+gold house/envelope mark) and wanted it in gold as the first thing in every invitation, the monogram circle removed/beautified, and more luxury. Couldn't embed the pasted raster (no file), so recreated it as a clean gold vector: new DawaLogo SVG (house body sweeping to a pin + roof/chimney + tilted envelope with wax seal + ring/diamond + heart + lower-left swirl), single themed gradient (theme.gradientStops) so it matches all 5 palettes; placed as the hero's first element with a rise+float+glow. Monogram: replaced the dashed circle around the couple initials with a delicate floral wreath (22 leaves + 11 berries) on a thin gold draw-animated ring. Kept the footer floral flourish + دعوة brand + lush petals from the prior turn. Pure presentational; verified live (mobile screenshot of /d/groom/{token}): gold logo crown + floral-wreath monogram render nicely (one blank run was the known deploy-propagation MIME race — resolved on re-run). Build passes; deployed hosting; committed ed89549. Noted to user they can drop the exact logo asset to swap the vector.

## [2026-06-04 02:20] session | Use the real Dawa logo image as the invitation hero badge
Touched: src/components/digital/DigitalInvitationView.jsx + public/dawa-logo.png (new). User supplied the real logo via a Discord CDN URL (expiring), so downloaded it (2816x1536, 6.2MB full mockup of the logo on a light card). Cropped centered on the logo graphic (System.Drawing crop x780 y139 w1300 h1214) and downsized to 520x486 PNG (~435KB) at public/dawa-logo.png so it's served same-origin (/dawa-logo.png; CSP img-src 'self' allows it). Replaced the prior gold SVG recreation (DawaLogo, removed) with <img className=dawa-inv-hero-logoimg src=/dawa-logo.png> shown as a rounded light badge (144px, theme.accentLine hairline border + soft shadow + 0..accentMuted glow) at the top of every invitation, keeping the rise+float animation. Since the logo is navy+gold it needs a light backing on the dark themes — the cropped card IS that backing, so it reads as an elegant logo coin. Kept the floral-wreath monogram + footer flourish + petals from prior turns. Verified live (mobile screenshot): logoImg present + loaded, renders cleanly above the wreath monogram, no errors (one transient blank run = deploy-propagation MIME race, handled via retry loop in the check). Build passes; deployed hosting; committed 9368e01.

## [2026-06-04 02:35] session | Invitation logo → site BrandLogo (gold)
Touched: src/components/digital/DigitalInvitationView.jsx; removed public/dawa-logo.png. Per user, switched the invitation hero logo to the SAME gold brand mark used across dawa.to — the BrandLogo component (src/components/BrandLogo.jsx → BRAND_ICON_SVG, the logo paths re-filled with the site gold gradient + soft aura/sealPop). Replaced the cropped-raster <img> badge with <BrandLogo size={96} />, removed the .dawa-inv-hero-logoimg badge CSS and the unused public/dawa-logo.png. Gold-on-dark needs no light badge. Verified live (mobile screenshot): brand SVG renders at the top of the invitation, no errors (note: on the test token the bg is a light uploaded image so the gold reads subtle; on dark themes/photos it pops). Build passes; deployed hosting. Two commits: 0ab315b (png delete) + 9a014a3 (jsx) — first commit accidentally only captured the png delete because git add hit the already-removed pathspec; jsx committed separately.

## [2026-06-04 03:05] session | Digital invitation luxe pass (very luxurious)
Touched: src/components/digital/DigitalInvitationView.jsx. User asked to make the digital invitation VERY luxurious ("اعمل الدعوة فخمة جدا!!!"). Added four robust, fully themeable touches that hold up on the mobile/in-app browsers where guests open WhatsApp links (deliberately NO clip-text/mask tricks — those mis-render, and .dawa-inv-grad is already forced to solid for that reason): (1) a faint damask diamond-lattice texture over the whole invite via a .dawa-inv::before tiled inline-SVG data-URI, stroke tinted with encodeURIComponent(theme.accent) at ~4.5% opacity (position fixed on public / absolute in preview, via new ixed prop on ViewStyles), reads as embossed stationery; (2) a gold filigree frame inset on the hero (.dawa-inv-hero-frame, border=theme.accent @ .4 opacity, 16px inset, 16px radius) with four heavier corner brackets (logical-property borders for RTL) + a draw-in keyframe; (3) a floral flourish (existing FloralFlourish) + a diamond-tipped gold rule under EVERY SectionHead; (4) a warmer accentMuted glow + inset hairline on the date/venue chip. Build passes; deployed hosting (SA key); verified live on mobile viewport (frame + corners visible, section ornaments + damask texture render, text stays readable). Committed 42b049f and pushed.

## [2026-06-04 04:10] session | Add 10 light palettes + Arabic/Hebrew font pairs
Touched: src/styles/digitalThemes.js, src/utils/digitalFonts.js (new), src/components/digital/DigitalInvitationView.jsx, src/pages/portal/groom/digital/DigitalDesignEditor.jsx, functions/src/api/routes/digital.ts, functions/src/digitalOgImage.ts. User asked (on /portal/groom/digital/design) to add ~10 more light luxurious colors + "all" fonts, Arabic-for-Arabic and Hebrew-for-Hebrew. Mapped the system first via a 4-agent Workflow (themes/editor/fonts/validator). Findings: THEMES/FONTS live in digitalThemes.js (pickers auto-derive from Object.keys); the ONLY hard gate on new keys is the backend validator Sets THEME_COLORS/FONT_FAMILIES in functions/src/api/routes/digital.ts (no firestore/RTDB rules restrict themeColor/fontFamily); OG image has an ACCENTS map keyed by themeColor. Implemented: (1) 10 new LIGHT palettes (champagne, blush, sage, dustyblue, lavender, pearl, peach, mint, mauve, ivorygold) — full 25-token objects from the white template, swatch=accent so the picker circles are distinguishable; 15 themes total. (2) Fonts reworked into 12 matched Arabic+Hebrew CSS-stack PAIRS (e.g. 'Aref Ruqaa','David Libre',serif) so the browser renders each script in its own face automatically — no per-language wiring. (3) Lazy scoped font loader src/utils/digitalFonts.js injects the ~17 extra Google families only when the invitation or the editor mounts (called via useEffect in both), so other pages don't pay; CSP already allows fonts.googleapis.com/gstatic.com. (4) Extended the two backend validator Sets + added 10 new accents to OG ACCENTS (brightened tones for the dark OG scrim). No DB/schema/rules changes. Built frontend + functions, deployed hosting+functions (SA key). Verified live via an injected groom session (custom token -> idToken): editor shows 15 themes + 12 fonts; Arabic shows كلمات in Arabic faces, Hebrew shows מילים in 12 distinct Hebrew faces; selecting lavender+aref PATCHed {ok:true} (validator accepts new keys); lavender light theme renders in both languages. Committed 8556a96 and pushed.

## [2026-06-04 04:45] session | Monogram: circle -> royal crown crest
Touched: src/components/digital/DigitalInvitationView.jsx. User (on /portal/groom/digital/design) wanted the circle around the «ح & س» initials removed and something fancier that grabs the eye. The old monogram was a thin gold ring (circle r=76) + a full 360-degree wreath (22 ellipses + 11 berries) encircling the initials. Prototyped 4 open-crest concepts in isolation via Playwright setContent (crown+sprigs, diamond cartouche, open laurel, horizontal lockup) rendered on dark+light bg, asked the user to pick -> chose "crown + sprigs". Implemented a polished version: a gold crown (3 tip-dots + a softly twinkling centre gem via dawa-inv-twinkle) above the initials, symmetric laurel sprigs flanking (left + mirrored group), and a small bottom flourish; no enclosing ring. All strokes/fills use url(#dawa-mono-<themeKey>) so it adapts to every palette; dots use theme.accent. Removed the dead .dawa-inv-ring CSS rule, added .dawa-inv-crown rise + .dawa-inv-crown-gem twinkle. Built + deployed hosting (SA key). Verified live (mobile, white theme token م&س): crown crest renders, initials stay readable (dark text), balanced hero. Committed deff6cc, pushed.

## [2026-06-11 02:30] session | Tidy groom dashboard header on mobile
Touched: src/styles/GlobalStyle.jsx, src/pages/portal/groom/GroomHandwrittenShell.jsx, src/pages/portal/groom/digital/DigitalPortal.jsx. User reported /portal/groom/handwritten/dashboard and /portal/groom/digital/dashboard look "very untidy" on phones. Reproduced via injected groom session (custom token -> idToken) + Playwright at 320/360/390. Diagnosis: NO horizontal overflow (scrollWidth==viewport); the untidiness was the HEADER — both portal shells crammed brand + 5 nav tabs + AR/HE LangSwitcher + type-switch (the changeType -> type-select button) + logout into a single flex row (height:54, justify space-between, gap 8, the right group flexWrap wrap) that wrapped into an uneven 2-line jumble on narrow screens. The card grids/lists below were already fine. Mapped the code with a 4-agent workflow (shell-header / digital-dash / handwritten-dash / global-styles). Fix (surgical, header only): restructured BOTH shells into three class-based groups — .dawa-phead (container) > .dawa-phead-brand + .dawa-phead-tabs + .dawa-phead-ctrl — removing the conflicting inline layout, and added the app's FIRST screen-size @media to GlobalStyle.jsx (none existed before, only prefers-reduced-motion): base classes give a single desktop row (brand | tabs centered | controls), and @media (max-width:620px) flex-wraps it into two tidy rows (brand+controls on top via order + margin-inline-start:auto on ctrl; tabs full-width centered below a hairline border-top), with a small nav-tab font/padding override (!important to beat the inline navStyle). Built + deployed hosting (SA key). Verified live on both dashboards at 320/360/390 (clean 2-row header, balanced body) and 760/1280 (single-row, centered tabs) — no cramping, desktop intact. Committed 5d2e6e8, pushed.

## [2026-06-11 04:00] session | Admin send: per-groom digital message + no-design
Touched: src/pages/portal/admin/AdminSendTab.jsx, src/hooks/usePortalState.js. On /portal/admin/send the admin wanted, after selecting a groom, a message box whose text is sent (via the existing wa.me flow) to each DIGITAL guest together with that guest's personalized digital invite link; plus a "بدون تصميم" option to send the message with NO link. Handwritten (with/without address) flows must stay unchanged. Clarified with the user: send like the handwritten flow (client wa.me, no backend), and one message for all with the guest name. Mapped the flow with a 4-agent workflow: the send is purely client-side buildWaLink(phone,body,url) in usePortalState (body+url joined by \n\n; empty url => message only), digital send mints a token via createDigitalGuestInvite then builds /d/{groomUsername}/{token}; the per-guest design dropdown calls assignGuestDesign. So the whole feature is FRONTEND-ONLY (no DB/rules/functions). Implemented: (1) AdminSendTab — new per-groom message state digitalMsgByGroom + a labeled textarea rendered at the top of the digital section (placeholder mentions {الاسم}); a personalize() helper substitutes {الاسم}/{name}/{שם} with the guest name; (2) added a "بدون تصميم (رسالة فقط)" option (sentinel NODESIGN="__nodesign__") as the first entry of the per-guest design <select>, always shown now (even when no approved designs → effDesignId defaults to NODESIGN); pickDesign skips persisting the sentinel (ephemeral override only); computed effDesignId/noDesign per digital guest; (3) usePortalState.sendDigitalInviteLink(guest, groomUid, customMessage, opts) — uses customMessage||adminDigitalMessage; when opts.noDesign it skips token minting and opens wa.me with the message only, else mints + appends the /d/ link. Handwritten sendInviteLink untouched. Built + deployed hosting (SA key). Verified live with an injected ADMIN session (custom token role:admin) on groom "groom" (5 digital guests, 3 approved designs) by intercepting window.open: with-design opened https://wa.me/<phone>?text=أهلا sdf dsf ... \n\n https://invite.dawa.to/d/groom/<token> (so {الاسم}->name + design link), and "بدون تصميم" opened the same personalized message with NO link. Committed a3754ee, pushed. NOTE: still-open / not-started requests from the user for a later session — (a) handwritten invite form: let the guest edit their name + phone so admin can detect a different responder; (b) surface the guest's shared/pinned LOCATION at admin Confirmations + driver guests-map; (c) surface the guest's note-to-driver at admin Confirmations + driver delivery list next to the name. These touch the public invite form + confirmation backend + guest data model (likely DB/rules changes) and need their own ask-first plan.

## [2026-06-11 05:30] session | Handwritten: detect different responder + show location & note
Touched: functions/src/api/routes/invites.ts, database.rules.json, src/pages/portal/admin/AdminConfirmationsTab.jsx, src/pages/portal/driver/DriverDeliveryList.jsx. Three asks on the handwritten invite/confirmation flow. Mapped via a 4-agent workflow + read the source. Findings: the public InviteForm ALREADY lets the guest edit name/phone (pre-filled from token, not locked) and already captures location (GPS/map pin) + a note; the gap was the backend + display. (1) Different-responder detection ("بزبطش"): buildPhysicalGuestPatch was OVERWRITING the guest record name/phone with the submitted values, so the admin Confirmations side-by-side (which matches by PHONE via matchUtils.classifyConfirmation and compares against the GUEST record) always looked identical -> always green. Fix: stopped writing name/phone in buildPhysicalGuestPatch (keep invited values; still update area/coords/deliveryNote/companions/confirmedAt). Now a changed name -> red "name_differs"; a changed phone -> no phone match -> "unknown". Admin applies entered values via the existing "use guest data" button. (2) Location: data already stored (guest.lat/lng + conf.lat/lng) and rules already allow it; the DriverMap already plots it. Added to AdminConfirmationsTab a "📍 الموقع الذي حدّده المعزوم" link (googleMapsLinkCoords) when conf.lat/lng present, and made the DriverDeliveryList Waze button use wazeLinkCoords(g.lat,g.lng) when present (was area-only). (3) Note-to-driver: guest's note lives on guest.deliveryNote but the driver's own delivery note (markGuestDelivered) OVERWRITES the same field, so to show it robustly to the admin I mirror it onto the immutable /confirmations record (buildPhysicalConfirmationRecord now sets deliveryNote) and show it in AdminConfirmationsTab ("📝 ملاحظة للمرسل"); DriverDeliveryList shows g.deliveryNote beside the guest name for pending guests. User picked the robust option, so this needed a RULES change: added an optional "deliveryNote" (string<=240) to the /confirmations schema in database.rules.json. The Admin SDK write bypasses rules so display works immediately, but the rules must be deployed for admin client edits of such confirmations to validate — SA key CANNOT deploy rules, so the USER runs: firebase deploy --only database --project dawa-aa793. Built functions+frontend, deployed functions+hosting (SA key). Verified E2E on prod: scripted a physical submit that changes the name + adds location + note -> guest.name kept original (PASS), conf.submittedName differs (PASS), conf.deliveryNote mirrored (PASS), conf.lat/lng mirrored (PASS); then an injected-admin Playwright screenshot of the Confirmations card showed invited «محمد المدعو الأصلي» vs entered «زائر مختلف» with «⚠ الاسم مختلف» + the «📍 الموقع» link + «📝 ملاحظة للمرسل». Committed 493d9e5, pushed. PENDING USER ACTION: deploy the database rules (above).

## [2026-06-11 06:10] session | Admin send: show all guests + confirmed/not status
Touched: src/pages/portal/admin/AdminSendTab.jsx. User wanted /portal/admin/send to keep ALL guests (digital + handwritten, confirmed or not) visible, with a small line under each saying confirmed-or-not. Previously the tab filtered out confirmed guests (selectedGroomGuests had `&& !g.confirmedAt`, digitalGuests `.filter(g => !g.confirmedAt)`) — they moved to the Confirmations tab. Changes (frontend only): (1) dropped both confirmedAt filters so every guest shows; (2) added a per-guest status line under the phone — isConfirmed = sec.digital ? g.status==="attending" : !!g.confirmedAt; isDeclined = sec.digital && g.status==="absent" — rendering "✓ تم التأكيد" (green) / "✗ اعتذر — لن يحضر" (red, digital absent) / "○ لم يؤكد بعد" (dim); (3) to avoid re-spamming confirmed guests, "send to all" now targets a new unconfirmedGuests = selectedGroomGuests.filter(!confirmedAt) and its count, instead of all. Built + deployed hosting (SA key). Verified live (injected admin, groom "groom"): status-line counts = 3 تم التأكيد, 9 لم يؤكد بعد, 1 اعتذر; confirmed digital guests (rff ftu, hfg rrr, مت اتا) + the declined one (dfg gfd) now appear; "إرسال للجميع (7)" counts only unconfirmed. Committed cc28640, pushed.

## [2026-06-11 06:45] session | Fix invite form name/phone revert (poll stale closure)
Touched: src/pages/InviteForm.jsx. User: on /portal/admin/confirmations «بيانات العريس» and «بيانات المعزوم» show identical (both the guest's data) even when they should differ; and on the handwritten invite form the guest can't change their name/phone — they type it and it gets erased and replaced with the invited name. ROOT CAUSE (one bug): InviteForm pre-filled name/phone inside the subscribeInviteToken callback with `if (rec && !name) setName(rec.guestName)`. subscribeInviteToken uses createPoller at POLL_MS.INVITES = 3s, so it RE-FIRES every 3s; the `!name`/`!phone` checks read a STALE CLOSURE (name/phone always "" from the first render, effect deps [token]), so every poll re-set the fields and wiped the guest's input. So the guest could never change name/phone -> submittedName ended up == invited -> Confirmations showed both sides identical. Fix: a `prefilled` useRef guard pre-fills name+phone ONCE on the first token record, then leaves the fields fully editable (removed the now-unneeded eslint-disable). Combined with last session's backend fix (buildPhysicalGuestPatch no longer overwrites guest name/phone), the guest's entered values now differ from the invited ones and the matcher flags the mismatch. Built + deployed hosting (SA key). Verified live E2E (Playwright on /invite/{token}): pre-filled "اسم العريس الأصلي", changed to "الاسم الجديد للمعزوم", it STUCK after 10s/3+ poll cycles (PASS); after submit guest.name stayed "اسم العريس الأصلي" while conf.submittedName = "الاسم الجديد للمعزوم" (DIFFERENT: PASS). Committed 04c987c, pushed.

## [2026-06-11 07:40] session | Confirmations groom-filter + digital +972/9-digit no-dup phone
Touched: src/pages/portal/admin/AdminConfirmationsTab.jsx, src/pages/portal/groom/digital/DigitalAddGuest.jsx, functions/src/api/routes/digital.ts. Two requests. Mapped via a 2-agent workflow; user clarified: (A) on confirmations, selecting a groom shows his guests + ALL unknowns + the DECLINED (digital absent); (B) digital phone duplicate-block is per-groom only. REQUEST 1 (frontend-only, no backend/rules): added a groom selector grid to AdminConfirmationsTab (reuses adminSelectedGroom/setAdminSelectedGroom + digitalGuestsForSelectedGroom from context, same as the Send tab). matched/mismatch are filtered by conf.groomUid === selectedGroomUid; unknown always shows all; a new "لن يحضروا (اعتذروا)" section lists (digitalGuestsForSelectedGroom).filter(status==="absent"). Before a groom is picked -> "اختر عريساً لعرض تأكيداته" hint. Confirmation records already carry groomUid+groomUsername; declined digital guests are NOT mirrored to /confirmations (the digital RSVP mirror removes them on absent) so they are read from the Firestore digital-guests subscription. REQUEST 2: digital add-guest phone. Frontend DigitalAddGuest.jsx: replaced the plain 10-digit input with a FIXED "+972" chip + a 9-digit numeric input (state holds 9 digits, stored canonically as "0"+9digits); subscribed to the groom's existing digital guests and added a client duplicate pre-check via toIntlPhone canonical compare (disables submit + inline "هذا الرقم مضاف مسبقاً"); handles backend 409. Backend digital.ts: added ilNational() (drops +972/leading 0 -> requires exactly 9 national digits); sanitizeDigitalGuestCreate now rejects bad format (invalid_phone) and canonicalises phone to "0"+nat; the POST /:uid/guests endpoint reads the groom's guests and returns 409 duplicate_phone if the same national number exists. NO rules change (Firestore app-layer). Built functions+frontend, deployed functions+hosting (SA key). Verified live: backend add->200, duplicate->409 duplicate_phone, bad-format->400 invalid_phone (PASS); confirmations UI shows the selector + "اختر عريساً" hint, selecting groom shows matched/mismatch/unknown, and a seeded absent guest rendered under "لن يحضروا" (then cleaned up); the +972 field renders with 9/9 green counter. Note: groom "groom" currently has only 3 digital guests (2 pending,1 attending) — earlier ones were removed by the user. Committed 8939d95, pushed.

## [2026-06-11 08:00] session | Scope unknown confirmations to their own groom
Touched: src/pages/portal/admin/AdminConfirmationsTab.jsx. Follow-up to the confirmations groom-filter: the user wanted an "unknown" (مجهول) reply for groom "groom" to appear ONLY under that groom, not under another groom. Previously matched/mismatch were filtered by the selected groom but `unknown` showed ALL globally. Since every confirmation record carries groomUid (the groom whose confirmation form/link was used), added `&& forGroom(c)` to the unknown filter so unknowns are scoped to the selected groom too. One-line frontend change; built + deployed hosting (SA key). Verified live: seeded an unknown confirmation for groom "groom" (phone matching no guest) -> shows under "groom" (PASS), hidden under "rani" (PASS); cleaned up. Committed 5c38ba8, pushed.

## [2026-06-11 09:30] session | Digital luxe layer + dashboard RSVP messages + drop companions
Touched: src/components/digital/DigitalInvitationView.jsx, src/pages/portal/groom/digital/DigitalDashboard.jsx, src/pages/InviteForm.jsx. User asked for 4 things; did 3 this turn, #2 (guestbook moderation) deferred to a plan (needs Firestore subcollection + rules). #1 (frontend): removed the "كم شخص/companions" question from the HANDWRITTEN invite form (InviteForm) — dropped CompanionsStepper import, partySize state, the UI block, and companions from the /invites/submit payload. #3 (frontend, display-only — NO backend/DB): the digital RSVP "رسالة للعروسين" is already stored as guest.note and the dashboard already subscribes to guests; added a "💌 رسائل المعزومين للعروسين" gold-card section at the end of DigitalDashboard listing guests.filter(g=>g.note) with name + attending/absent badge + the message, shown ONLY to the groom (الرئيسية). #4 (frontend, the priority — make the digital invitation feel 20-star luxurious): added a LUXE LAYER to DigitalInvitationView — (a) a living gold "aurora" (3 blurred accent radial blobs drifting via dawa-inv-drift1/2/3) added in <Ambience> behind everything; (b) richer gold dust (sparkles 20->34 with random size + size-scaled glow); (c) layered gold halos on .dawa-inv-couple + .dawa-inv-greet strong (text-shadow stacks; appended after originals so overrides win); (d) .dawa-inv-detail items turned into glass cards (theme.cardBg + cardBorder + blur + gold glow); (e) deeper glow on venue-map / countdown / dateline / hero-frame; (f) gleaming section diamond dot. All theme-token driven, robust (no clip-text — respects the .dawa-inv-grad solid override). Built + deployed hosting (SA key). Verified live via a TEMP gold-theme token (cloned a5f1 designSnapshot, themeColor=gold, fontFamily=aref, minted + screenshot mobile + deleted): hero shows aurora + crown crest + haloed couple names + gold dust; detail section shows glass cards. #1: seeded physical token, invite form has NO companions question (PASS). #3: seeded a note on an attending guest, dashboard shows "رسالة اختبار" under رسائل المعزومين (PASS), reverted. Committed 4fd12f5, pushed. PENDING: #2 guestbook moderation plan + user approval (new Firestore digitalInvitations/{uid}/wishes subcollection w/ status pending/approved + a Firestore RULES change the user must deploy + backend submit/approve/public-filter endpoints + groom moderation UI + cross-design live render of approved wishes).

## [2026-06-11 11:00] session | Guestbook wish moderation (submit -> approve -> public carousel)
Touched: functions/src/api/routes/invites.ts, functions/src/api/routes/digital.ts, src/services/invites.js, src/services/digitalInvitation.js, src/pages/DigitalInvitationPage.jsx, src/components/digital/DigitalInvitationView.jsx, src/pages/portal/groom/digital/DigitalDesignEditor.jsx. Implemented #2 from the earlier 4-ask batch with the user's refinements (approval = instantly public, no separate publish button; groom can un-publish; display 3-at-a-time rotating carousel). KEY: NO Firestore rules change needed — all wishes access is via the REST API (Admin SDK bypasses rules); the client never reads the wishes subcollection directly (firestore.rules default-deny is irrelevant). Backend: new Firestore subcollection digitalInvitations/{groomUid}/wishes {who, what, status: pending|approved, guestId, submittedAt, approvedAt}; POST /invites/digital/wish (public, token-based, ipRateLimit) writes pending; GET /invites/digital/wishes/:token (public) returns APPROVED wishes live (so they show across all designs/tokens, not the frozen snapshot); GET/PATCH/DELETE /digital/:uid/wishes[/:wishId] (canActOnUid) for moderation (+ wishesCol helper). Frontend services: submitDigitalWish + getApprovedWishes (invites.js); subscribeDigitalWishes + setWishStatus + deleteWish (digitalInvitation.js). DigitalInvitationPage: fetches approved wishes on load -> approvedWishes prop; handleSubmitWish -> submitDigitalWish. DigitalInvitationView GuestbookSection rewritten: submit -> onSubmitWish then shows "بانتظار موافقة العروسين"; merges approved guest wishes + groom-authored design.wishes into a carousel showing PER=3 with a 5s auto-rotate interval + clickable page dots + dawa-inv-rise fade on page change. DigitalDesignEditor: a "💌 تهاني المدعوين" gold-card panel after the StatusBanner — pending wishes (✓ موافقة ونشر / ✕ رفض) + approved wishes (إلغاء النشر), optimistic local updates. Built functions+frontend, deployed functions+hosting (SA key). Verified live E2E (token a5f1, groom "groom", admin approve): submit->200, stored pending, hidden before approval, approve->200, visible after approval (all PASS); seeded 4 approved -> public guestbook shows exactly 3 at once (PASS). Committed 34c89c2, pushed. ALL FOUR asks from the batch now done (#1 companions removed, #3 dashboard RSVP messages, #4 luxe layer, #2 guestbook moderation).

## [2026-06-11 21:04] session | Login "broken" was a password mismatch, not code
Touched: Authentication. User reported can't log in as admin/groom/driver on the live site ("wrong username or password", worked-before-broke-recently) and asked to fix the functions-folder auth code. DIAGNOSIS (outside-in, read-only first): the API code is healthy and correctly deployed — GET /api/health → 200 (same-origin + direct Cloud Run); POST /api/auth/login with bogus creds + Origin: https://dawa.to → clean 401 invalid_credentials, which proves CORS + WEB_API_KEY + the Firebase Auth proxy all work (a 500/server_misconfigured or API_KEY_INVALID would mean otherwise). syntheticEmail lowercases identically on create vs login (no mismatch). backup-mrwen (current) carries the FIXED CORS (cb(null,false) not throw) + 50/hr rate limit; main still has the old CORS-500-throw — but the live function already runs the fixed path. A temp read-only audit (Admin SDK key + listUsers + /usernameIndex + /users) showed all 5 portal accounts (admin, groom, driver, rani, rawad) exist, consistent, enabled, each signed in TODAY. Live probe of the seed creds: driver/Driver1234 → 200 (works!), admin/Admin1234 + groom/Groom1234 → 401 (prod passwords had drifted from seed defaults). ROOT CAUSE: not a code bug and not account state — the admin/groom passwords being typed were simply wrong. FIX (user approved the Auth write): node functions/scripts/resetUser.js for admin + groom to new known passwords (revokes old sessions, preserves role claims). VERIFIED: live POST /api/auth/login → 200 for all three roles with correct role; Playwright browser login as admin on https://dawa.to landed on /portal/admin/users with the user-management panel rendered. NO code changed (temp audit script created + deleted; working tree clean → nothing to commit/deploy). New admin/groom credentials delivered in chat only (kept out of committed files); recommended rotating + storing in a password manager. Added a "Troubleshooting can't log in" playbook to wiki/Authentication.md.

## [2026-06-11 21:30] session | Full-stack security audit + fixes (Tiers 1–3 + rules)
Touched: Security Model, Authentication, Known Bugs. Ran a recon (3 Explore agents) + an adversarial verification workflow (17 agents: 12 candidate findings confirmed/refuted + 5 fresh-hunt) to produce a consolidated report, then fixed the prioritized list. CONFIRMED-REAL findings fixed (all code-only, deployable with SA key): **T1** rate-limiter IP — `resolveClientIp` no longer trusts the spoofable leftmost X-Forwarded-For; uses Express `req.ip` with `trust proxy: TRUSTED_PROXY_HOPS` (default 1) in `api/index.ts` (the leftmost-XFF trust let an attacker mint a fresh bucket per request, defeating login/OTP/confirm limits). **T2** physical `POST /invites/submit` now enforces a one-shot `usedAt` guard via an atomic transaction-claim (+ rollback on write failure) — was replayable for the token's 90-day TTL, pushing duplicate /confirmations rows. **T3** `POST /auth/logout` now calls `revokeRefreshTokens` (was a no-op) — verified the user's `tokensValidAfterTime` advances; `requireAuth` already uses `verifyIdToken(token,true)` so old tokens are rejected in prod (emulator doesn't enforce revocation). **T4** `POST /live-locations` filters `shareWith` to the driver's `assignedGrooms` claim (clear stays unfiltered for stale-share cleanup). **T5** `GET /digital/:uid/public` is now approved-only on BOTH paths (legacy path leaked draft designs). **T6** `GET /invites/token/:token` returns a projected subset (drops internal groomUid/guestId/createdAt; keeps guestName/guestPhone/guestType/designSnapshot for prefill+render). **T8** `?token=` query auth restricted to GET at runtime. **T9** public confirmation auto-attach no longer mutates a guest's GPS pin (coords stay on the immutable /confirmations record for admin attach). **F12** `PATCH /users/:uid` field allowlist (displayName only). **F13** attach-location id charset validation. **F14** login/OTP return generic codes (USER_DISABLED folded into invalid_credentials). **F15** proof upload validates guestId charset + existence. **F16** wishes capped at 5/guest. **F17 (now implemented, not deferred):** per-account login lockout — `rateLimit.ts` gained `failureCount`/`recordFailure`/`clearFailures` and `auth.ts` blocks an account after `LOGIN_MAX_FAILURES_PER_ACCOUNT` failed logins/hr (independent of the spoofable IP key; `IN_EMULATOR`-guarded so e2e can hammer login). Also added a `[security] driver_self_assignment` structured Cloud Logging line in assignments.ts for assignment-burst alerting. **Deps (T7/T13):** added functions `overrides` for @grpc/grpc-js ^1.14.4 (2 HIGH DoS), uuid ^11.1.1, qs ^6.15.2 → `npm audit` clean. **T11 CORS:** fail-closed allowlist with always-allow localhost + `*.web.app`/`*.firebaseapp.com`; ALLOWED_ORIGINS set in functions/.env (gitignored) to dawa.to/www.dawa.to/invite.dawa.to/*.web.app/*.firebaseapp.com. **T12** Leaflet CDN injection pinned with SRI (sha384, verified vs published sha256). **UI:** SharedCities delivery now updates `sharedGuests` optimistically (was lingering ~15s); removed dead `<EditUserModal>` + dead `sections` array; GroomDashboard "last deliveries" empty state; aria-labels on login/add-guest inputs + the MediaTile ✕ button. **RULES (user must deploy):** `database.rules.json` — `inviteTokens/$token` `.read` true→false (R1; server Admin SDK only), and `liveLocationsByGroom/$groomUid/$driverUid` `.write` now also requires an existing driverAssignment to write DATA while still allowing null/clear (R2); updated tests/rules/database.rules.test.js to match + added positive/negative cases. **REFUTED (no change):** canActOnUid IDOR (strict), digitalBaseUrl scheme injection (https enforced), OTP-reset takeover (phone-bound), and the three 2026-05-28 QA criticals (all already fixed). **DEFERRED (architectural, documented):** localStorage→HttpOnly-cookie refresh, SSE token-in-URL→ticket, shared-store rate limiter, face-api node-fetch, firebase-admin 13→14, CSP unsafe-inline. Surfaced + fixed a latent clean-build error in guests.ts (delete on a non-optional key). VERIFIED: functions clean build + frontend build PASS; unit 432 pass (2 pre-existing apiClient env failures, unrelated); integration/rules 81/81; Playwright e2e 56 pass/1 skip/0 fail; emulator API checks for F2/F3/F6/F7 PASS. Closed BUG-O004 (invite links now exercised end-to-end). PENDING USER ACTION: deploy rules — `firebase deploy --only database --project dawa-aa793` (SA key cannot deploy rules); smoke-test the groom live-map (SSE/CORS) and a login after the functions deploy.

## [2026-06-12 07:44] session | server-side face index for guest photos
Touched: Face Matching, Digital Invitations, index

## [2026-06-12 09:20] session | Refined mobile luxury redesign prompt
Touched: none

## [2026-06-12 14:50] session | Search + filter chips on every portal list
Touched: List Search and Filter (new), index. Added a reusable client-side search box + status/category filter-chips to all 12 list-rendering pages across the Admin/Groom/Driver portals. Foundation: exported the previously-internal `normalizeText` from utils/matchUtils.js; new utils/searchFilter.js (`normalizeForSearch` = westernize digits + fold diacritics + strip Arabic hamza marks U+0653–U+0655 so search is hamza-insensitive; `matchesQuery` = token-AND substring with phone-aware matching via normalizePhoneForMatching; `filterList`; `useListFilter` hook returning {query,setQuery,activeStatus,setActiveStatus,filtered,counts,chips}); new components SearchBar.jsx (🔍 + X/Y count pill + × clear, RTL clone of GroomMultiSelect's box) and FilterChips.jsx (scrollable gold pill row, live counts); ~32 i18n keys in ar.js+he.js (search_*_placeholder, search_no_results, filter_all, chip_* status labels). Wiring done via a 24-agent Workflow (one wire + one independent verify/fix agent per page) — universal rules: filter BEFORE grouping, metrics stay on the full array, compose with existing pre-filters (role tabs/groom selector/mode), module-level field/statusOf constants. Chip dimensions: delivery status (GroomGuests, DriverDeliveryList, SharedCities), RSVP (DigitalGuests, DigitalDashboard messages, AdminSendTab digital), role (AdminUserManager — existing tabs are the chips, search-only added), match status (AdminConfirmations), design status (AdminDesigns), file type (DigitalPhotographer); search-only GroomDashboard + GroomProofs. QA fix found live: AdminSendTab's RSVP chips (bound to the digital list) showed 0/0/0 above physical guests in manual mode → now gated on digitalGuests.length>0. Verified: 24 new unit tests (376 total pass); Vite build clean (344 modules); Playwright on emulator (seed admin/groom/driver) — GroomGuests full (text + phone-fragment "2222"→Fatima + delivery chips with live counts + section recompute + no-results notice + total preserved), AdminUserManager (search composes with role tabs, 1/3), AdminSendTab (search + chip-gating fix). Driver delivery list gated behind an unrelated groom-assignment claim in the emulator; same shared primitives verified elsewhere. NOT yet committed/deployed.

## [2026-06-12 14:55] session | repo re-layout into frontend/backend + monolith splits + dead-code purge
Touched: none

## [2026-06-13 12:00] session | Full product/UX/eng audit (14-agent workflow)
Touched: Product Audit 2026-06-13 (new analysis page), index.md

## [2026-06-13 19:30] session | Load-test control dashboard + always-on cleanup
Touched: Load-Test Dashboard (new), index. Built a local-only FastAPI+React dashboard for the Locust suite (loadtest/). A parallel session had landed most of it in d1cc21d (Python config-file overrides in locustfile.py, cleanup_core.py, new admin.ts DELETE /api/admin/loadtest-data + cleanup-loadtest.cjs, the React components) but the build hit the session limit before writing App.jsx and the whole FastAPI backend. This session wrote the missing FastAPI backend (dashboard/: runner.py spawns Locust as a web-mode subprocess + polls /stats/requests → SSE; cleanup.py always-on 3-step cleanup; history.py archive/timeseries/compare; configstore/schemas/api/main), the App.jsx shell, committed presets, .env.example, and start-dashboard.ps1. Never deployed — binds 127.0.0.1. Cleanup runs after every run/abort/crash: groom-API guest+wish deletes + the admin purge endpoint (confirmations/inviteTokens/guests/wishes, LOADTEST-marked only) + artifact rotation; out/ keeps latest, Archive copies to out/runs/<ts>/. Verified end-to-end on the Firebase emulator with Playwright: config flow UI→server→Locust, live charts (RPS/users, p50/p95 w/ SLO line, error%), live endpoint table, create→delete cleanup roundtrip (1 guest/5 wishes/1 confirmation/1 token created then all removed), verdict + 4 PNGs, archive, history, compare overlays, prod-confirm modal (428-gated, disabled until hostname typed). Fixed 2 Locust-2.44 mismatches found in testing: per-endpoint p95 key response_time_percentile_0.95; per-interval user count = max across rows (Aggregated row reports 0 under LoadTestShape). Commits: a4159c8, dd72fc8, 4a4a2d0. NOT yet deployed — DELETE /api/admin/loadtest-data needs `firebase deploy --only functions` for prod cleanup to work.

## [2026-06-13 20:35] session | Implement audit Phase 1 + 2 (errors, comms, WhatsApp)
Touched: Product Audit 2026-06-13. Phase 1 quick wins (localizeApiError layer + i18n error keys, share_invalid Telegram->WhatsApp copy, isProofImage shared helper, softened reminder/emergency promises, backend detail-leak guard via DAWA_DEBUG_ERRORS, adminSettings RTDB .validate fix). Phase 2 comms (req 1+3): /adminSettings contact fields + validation + public GET /settings/public, RTDB rules, CONTACT config fallback + utils/contact.js, admin Communication settings UI, landing-page pricing/CTA/footer WhatsApp wiring + Terms contact. Builds + 408 FE / 116 BE unit tests green. Not committed/deployed.

## [2026-06-13 22:20] session | Implement audit Phase 3 (Stripe payments)
Touched: Product Audit 2026-06-13. Stripe payment links (test mode) + webhook, scaffolded for later keys. New backend/functions/src/api/routes/payments.ts (POST /payments/:uid admin creates on-the-fly Price + Payment Link; POST /payments/webhook verifies Stripe-Signature via Node crypto, marks groom paid via stripePaymentLinks reverse index). Mounted in api/index.ts. Payment fields on /users + RTDB .validate rules. Frontend services/payments.js + PaymentSection in AdminUserManager edit modal (plan picker, create link, copy, send-on-WhatsApp, status badge) + i18n. Reads STRIPE_SECRET_KEY/STRIPE_WEBHOOK_SECRET from env (503 until set). Builds + 419 FE / 128 BE unit tests green (5 new webhook-signature tests). Not committed/deployed.

## [2026-06-13 22:55] session | RSA password-encryption layer (committed dc22fd6, deploy held)
Touched: Authentication, Security Model. Added a defense-in-depth password-encryption layer across all 3 runtimes: client RSA-OAEP-encrypts password fields as `enc:v1:<base64>` envelopes; backend `decryptPasswordFields` middleware decrypts in place ahead of /auth + /users; GET /auth/pubkey serves the SPKI key; /health reports `encryption`. Backend passwordCrypto.ts (env PASSWORD_ENC_PRIVATE_KEY, ephemeral dev key, no-key=disabled), frontend passwordCrypto.js (WebCrypto, fail-safe never-throws, stale-key + encryption_required retry), loadtest passwordcrypto.py (+cryptography). Backward-compatible; gated by PASSWORD_ENC_PRIVATE_KEY + REQUIRE_ENCRYPTED_PASSWORDS. 14-agent adversarial review → fixed over-length-pw crash, pubkey cache staleness, encryption_required retry, /health signal. Tests FE 419 / BE 128 / loadtest green. Live emulator + Playwright verified browser sends enc:v1 (login 200). Committed ONLY encryption files (dc22fd6) — parallel payments/contact/rules work left uncommitted per user; SessionEnd auto-deploy temporarily DISABLED in settings.local.json to hold the deploy.

## [2026-06-13 22:55] session | Deploy audit Phases 1-3 to production
Committed audit Phases 1-3 only (9a2a12d) — password-encryption work left unstaged for the user. Deployed database rules + functions + hosting to dawa-aa793. Smoke test caught a route-shadowing bug (POST /payments/webhook hit /:uid → 401); fixed by ordering /webhook before /:uid (fa5d3bb), redeployed, re-verified (webhook→503 stripe_not_configured, admin route→401, /settings/public→200, /api/health ok). Live at https://dawa-aa793.web.app.

## [2026-06-13 23:25] session | Audit Phase 4a — WhatsApp Cloud API core
Added whatsapp.ts (sendWhatsAppText + verifyWebhookChallenge, env-gated) and a standalone whatsappWebhook onRequest (GET verify / POST receipts), exported from index.ts. Converted backend unit test include to a glob (explicit list was silently dropping my Phase 3 paymentsWebhook test). 419 FE / 139 BE tests green. Committed 50c53ed; not yet deployed (deploys with next functions deploy). Needs WHATSAPP_TOKEN/PHONE_ID/VERIFY_TOKEN to go live.

## [2026-06-14 00:05] session | Audit Phases 4b-13 built + deployed
Autonomous run through the rest of the roadmap, each phase verified (build+tests) then committed + deployed: 4b reminder scheduler (onSchedule), 5 groom onboarding checklist, 6 driver non-delivery outcomes, 7 bulk guest import, 8 audit-log reader, 9 dashboard reply rollup, 10 "made with Dawa" growth loop, 11 PII consent links, 12 global Hebrew fonts + mixed-language fix, 13 CSV export + Waze. 431 FE / 147 BE tests green. functions+rules+hosting deployed to dawa-aa793. Deferred (needs owner sign-off): 30-day PII auto-deletion (policy rewrite vs destructive cron); server-side invite-open analytics. WhatsApp Business + Stripe live once their credentials are set.

## [2026-06-14 00:15] session | Wiki: record audit + Phases 1-13 implementation
Touched: Audit Remediation 2026 (new), Payments (new), WhatsApp Messaging (new), Communication Settings (new), Product Audit 2026-06-13, index.md

## [2026-06-14 00:30] session | Wiki: record password-encryption layer
Touched: Password Encryption (new), Security Model, Authentication, index.md. Documented the RSA-OAEP password-encryption layer (enc:v1: envelopes, /auth/pubkey, decrypt middleware, PASSWORD_ENC_PRIVATE_KEY + REQUIRE_ENCRYPTED_PASSWORDS gating, passive-only threat boundary, no forward secrecy, /health encryption signal). Cross-linked from Security Model + Authentication; added to index Concepts. Committed dc22fd6, deploy held; inert in prod until the secret is provisioned.

## [2026-06-15 05:55] ingest | server-side face index feature
Touched: Face Matching, Data Storage Model, Security Model, API Contracts, Architecture Decisions, Known Bugs, Tasks Backlog, index

## [2026-06-15 12:30] session | Face recognition rebuilt on AWS Rekognition + People gallery (deployed)
Touched: Face Matching, index.md. Replaced the face-api/WASM engine with AWS Rekognition behind a config seam (faceIndex/config,rekognition,cluster,clusterJob,purge.ts; legacy face-api kept as no-creds fallback). Personal "your photos": upload OR AWS Face Liveness selfie → SearchFaces matches → server ZIP; phone capture. New OTP+guest-list gated "People" gallery (/g/:groomUsername): union-find clustering → person tiles inheriting the invitation theme → per-person photos+ZIP; admin console (AdminGallery) for approve/kill-switch/curate(hide/rename/merge)/monitor; WhatsApp template bulk-send; biometric 30-day auto-purge + admin erase. Thresholds calibrated from real photos via rek-diagnose (match/cluster 90; clean 85-95 gap; example fixtures are graduation group shots → ~22 people). Provisioned AWS IAM user + Cognito pool (docs/AWS_REKOGNITION_SETUP.md). Verified: `npm run test:rekognition` (recall + cluster separation) + emulator/real-Rekognition e2e (verify-faces-e2e + verify-gallery-e2e, both pass) + 438 FE / 173 BE unit tests. Committed 6bc5b3a..5f3b900 (11 commits) and DEPLOYED to dawa-aa793 (functions+rules+hosting); live smoke green. Owner follow-ups: prod VITE_RECAPTCHA_V2_SITE_KEY shows "Invalid key type" (fix before enabling gallery OTP); WhatsApp "your photos" template needs Meta approval; gallery kill-switch is per-wedding default-off; groom self-service gallery editor deferred (admin console covers operations).

## [2026-06-19 15:45] session | 4 improvements: phone input, camera-only, contacts import, auto-send
Touched: Digital Invitations, Face Matching, index.md. Built 4 requested items: (1) both guest phone fields (RSVP InviteRsvp.jsx + DigitalYourPhotos.jsx) now reuse the shared PhoneInput → fixes Arabic-digit drop bug, adds country picker + live ✓. (2) "your photos" enrollment is camera-only — removed the upload button/file-input/enrollSelfieUpload; AWS Face Liveness is the only path with a graceful "needs camera" notice when livenessConfigured() is false. (3) vCard/CSV contacts import on the digital add-guest (new utils/contactsImport.js + 7 unit tests) feeding the existing parseGuestLines preview/dedup/batch-add. (4) auto-send "your photos ready" on publish — extracted sendPhotoLinksForGroom(), wired to the photographerPublished flip via galleryConfig.autoSendOnPublish, added a groom toggle on the photographer page; gallery PATCH no longer demotes approved gallery for operational flags. Verified: frontend build + 445 FE / 185 BE unit tests green + Playwright MCP against emulator (vCard import added 2 guests + flagged 1 dup; Arabic digits ٠٥٢٤... → 52-426-4094 with ✓; auto-send toggle persists across reload; camera-only page mounts with no upload text). FOLLOW-UP (a) Cognito — RESOLVED: drove the AWS console via Playwright (owner logged in); the pool ALREADY existed (dawa_face_liveness, us-east-1:f2e0f5cc-6027-4859-94d5-3b9cdab221da, guest access active, role dawa-cognito-unauth w/ rekognition:StartFaceLivenessSession) — just never wired. Added VITE_AWS_REGION=us-east-1 + the pool ID to frontend/.env (gitignored) + recorded in docs/AWS_REKOGNITION_SETUP.md; verified the pool ID is in the built LivenessCapture bundle so the camera button now appears. Ships via hosting-only deploy. (b) A foreign in-progress analytics feature (admin.ts +96 lines + untracked backend/functions/src/api/analytics/) is in the working tree (OneDrive sync from another session) and breaks `tsc` (unused COLL_DESIGNS) — NOT mine, blocks the functions build/deploy; staged only my own files. NOT yet deployed.

## [2026-06-19 15:55] session | Build admin analytics page (server-aggregated + recharts)
Touched: Admin Analytics (new), index.md. Built the missing admin "📊 Analytics" command center end-to-end: new pure aggregator backend/functions/src/api/analytics/aggregate.ts (composition/revenue/operations/rsvp/designs/triage/trends) + GET /admin/analytics in admin.ts (requireAdmin + uidRateLimit + 45s cache, ?window=30d|90d|all, single sharded read mirroring guests.ts + collectionGroup designs/guests) + 12 unit tests. Frontend: recharts (^2.15, lazy-loaded → own 116KB-gz chunk), services/analytics.js, AdminAnalytics.jsx (donuts/funnel/leaderboard/multi-line trends, each chart in dir=ltr wrapper for RTL, bilingual tt() labels, reuses Num/ProgressBar/SkeletonList/C.*), nav tab + route in AdminPortal.jsx. Honesty guards: no delivery time-series (deliveredAt is HH:MM string), no invite-open rate (untracked), no fabricated "failed" payments. Verified: 445 FE / 185 BE unit tests green, functions tsc clean, frontend build clean, Playwright MCP against emulator with enriched data (all 7 sections, 20 recharts surfaces, revenue 3500₪/headcount/3 triage items correct, AR↔HE toggle switches every label). NOT committed/deployed (parallel OneDrive session committed unrelated "4 improvements" as 7f70c40; my analytics files left uncommitted on top, intact + compiling). Optional follow-up: instrument invite-open tracking for a full RSVP funnel.

## [2026-06-19 16:30] session | Wire Cognito Liveness pool (camera-only unblocked) + record to wiki
Touched: Face Matching, Digital Invitations, index.md, docs/AWS_REKOGNITION_SETUP.md. Drove the AWS console via Playwright (owner logged in) to fix the "no camera button" issue from the earlier camera-only change. Discovered the Cognito Identity Pool ALREADY existed from the 2026-06-15 setup and was correctly configured — name dawa_face_liveness, ID us-east-1:f2e0f5cc-6027-4859-94d5-3b9cdab221da, guest access active, role dawa-cognito-unauth with the DawaFaceLiveness policy granting rekognition:StartFaceLivenessSession. So nothing needed creating; it was simply never wired to the frontend. Fixed by adding VITE_AWS_REGION=us-east-1 + the pool ID to frontend/.env (gitignored) and recording the real values in docs/AWS_REKOGNITION_SETUP.md (corrected the eu-west-1→us-east-1 placeholder). Verified the pool ID is baked into the built LivenessCapture bundle, so livenessConfigured() is now true and the camera button appears. Committed 457719e. Deploy now fully unblocked: the parallel analytics session committed 4ae4ac8 in a compiling state, so functions tsc is clean and both features (my 7f70c40 + analytics 4ae4ac8) are on main; frontend + functions builds both clean. Remaining: live Face-Liveness smoke-test on the deployed site with a published invitation.

## [2026-06-19 16:20] session | Commit + deploy admin analytics to prod
Touched: Admin Analytics, Dawa, Audit Remediation 2026, index.md. Shipped the admin analytics command center built earlier: staged only my 11 files (never blind add -A — admin SDK key in repo root), committed 4ae4ac8 (feat(admin): analytics command center — server-aggregated /admin/analytics + recharts). Per user choice, fast-forwarded main to include both the parallel session's 7f70c40 (digital improvements) and 4ae4ac8, pushed origin/main (d7f20a7..4ae4ac8). Deployed everything to dawa-aa793 (functions incl. api, hosting, firestore rules) — Deploy complete. Prod smoke-test (cache-busted): landing 200, /api/health ok, /api/admin/analytics → 401 unauthenticated (route deployed + admin-gated). Live at https://dawa-aa793.web.app/portal/admin/analytics. Wiki: updated Admin-Analytics status→SHIPPED, added inbound links from Dawa hub (Operations) + Audit-Remediation-2026 (closes the "no analytics" gap; only public-hot-path invite-open tracking remains deferred).

## [2026-06-20 12:10] session | CRO/IA audit + ship WhatsApp-primary CTA flip
Touched: CRO and IA Audit 2026-06-20, Competitor Landscape, Buyer Persona, Conversion KPIs, Communication Settings, Admin Analytics, index.md, frontend/src/pages/LandingPage.jsx, frontend/src/i18n/{ar,he}.js. Ran a web/conversion + IA audit (assignment: discovery/research, IA, CRO). Owner answers anchored verdicts: keep high-touch sales, primary action = WhatsApp message, blind on competitors/persona. Headline finding: landing page funneled the dominant 6x-repeated gold CTA into a /portal login wall a prospect cannot pass, while the wanted action (WhatsApp) was a buried ghost button absent from the hero. Shipped (verified AR+HE via Playwright MCP, build clean 3769 modules): (1) flipped CTA hierarchy to WhatsApp-primary across hero/topnav/personalization/final, demoted portal to "already a client? log in" text link; (2) removed empty "AS FEATURED" credibility claim; (3) surfaced live sample invite link -> /d/demo/demo?demo=1 (reuses ?demo=1, verified renders); (4) added SocialProofSection reading i18n `testimonials` (empty -> renders null; NOT fabricated, owner pastes real consented quotes); (5) replaced hero trust-chips with real differentiators (live driver tracking, photo delivery proof, guest photo gallery, 24h). CRITICAL: prod /api/settings/public returns {} -> no WhatsApp number set, so every "book" CTA currently falls back to login (graceful, no regression, but funnel inert until owner sets the number in admin Communication settings or VITE_CONTACT_WHATSAPP). Discovery deliverables written to wiki: Competitor Landscape (digital invite is commodity vs free tools; delivery+proof+photo-finder is the moat; objection table + real named competitors QuikRSVP/Pippit/Mahabba/Save-This-Date), Buyer Persona (organized-bride decider + family-as-payer; needs validation), Conversion KPIs (North Star paid-weddings/month + funnel + cadence). Deferred with reasons: top-of-funnel WhatsApp-click tracking (needs DB-writing endpoint -> separate go-ahead), real testimonial content (owner-supplied), self-serve signup/checkout (against high-touch model). Not yet committed at log time — staging specific files next.

## [2026-06-20 09:20] session | Visual-design audit + ship "keep & refine" pass
Touched: Visual Design System (new), index.md, frontend/index.html, frontend/src/styles/{theme.js,GlobalStyle.jsx}, frontend/src/components/{CityField.jsx,StreetField.jsx,icons/Icon.jsx}, frontend/src/pages/LandingPage.jsx, frontend/public/{favicon.svg,site.webmanifest}. Assignment: product/visual-design & interaction audit (custom design, original assets, polish, motion, loading). Grounded current-state via 3 parallel Explore sweeps — headline: Dawa ALREADY ships bespoke premium design (gold/dark palette, 16 invite themes, custom SVG seal+monogram, rich guest-invite motion), so verdicts were "keep & refine"/skip, not build. Owner answers: budget open (ROI-gated), invest both surfaces, keep aesthetic. Shipped 4 build-verified commits (d7b085b, aa7baf1, d900a47): (1) index.html pre-mount #dawa-splash kills white→black cold-load flash + fonts moved @import→<head> <link> preconnect; (2) favicon.svg + site.webmanifest + theme-color from the seal; (3) theme.js token scales space/radius/type/shadow/z (matched to existing literals, additive); (4) WCAG AA contrast bump C.dim #7a6a4a→#8a7a58 (~3.8→4.8:1); (5) .menu-item/.tap hover-press utilities + fixed janky onMouseEnter style-mutation in CityField/StreetField; (6) dependency-free components/icons/Icon.jsx (mail/mobile/clock/gem/check/menu/close, currentColor) replacing landing emoji — mail=handwritten, mobile=digital. Honesty correction: audit's "Toast unused" was wrong — usePortalState.showToast wires it portal-wide; public forms intentionally keep inline errors (no change). Verified via Playwright MCP on built landing: dark cold-load no flash, 8 gold line icons in RTL order, favicon/manifest 200, fonts via head link, dir=rtl. Deferred (reviewable): app-wide functional/status emoji sweep + footer social, illustration commission (ornaments/empty-states/OG card — only paid spend worth it early; photography lowest-ROI), Card/Modal/Stack/Field primitives, landing scroll-reveal parity, Suspense-fallback standardization. Plan file: ~/.claude/plans/role-you-are-a-wild-kitten.md. Not deployed (SessionEnd hook owns deploy).

## [2026-06-20 12:20] session | Execute analytics/QA/web-ops audit remediation
Touched: QA Analytics and Ops Hardening 2026-06-20 (new), index.md, docs/{RUNBOOK,KPIS,ONBOARDING,USER_TESTING,SMOKE_TEST}.md, CHANGELOG.md, backend/functions/src/{index.ts,backupRtdb.ts}, backend/scripts/setup-backups.sh, backend/tests/rules/{firestore,storage}.rules.test.js, frontend/{package.json,playwright.config.ts,e2e/a11y-axe.spec.ts}, package.json, .github/workflows/ci.yml. Executed the in-repo half of the analytics/QA/handoff audit (owner said "execute the plan"; owner answers from the audit: analytics=GA4+heatmaps, maintainer=hiring a co-maintainer, QA reach=real users+several real devices+solo phone/emulator). Shipped additive, zero-prod-behavior-change work, committed 502e33f (16 files, never blind add -A — admin SDK key in repo root): (1) DR backups — backupRtdb scheduled Cloud Function (daily RTDB->GCS, no-op until BACKUP_BUCKET set, mirrors reminders pattern) + setup-backups.sh (Firestore PITR + managed daily backups, Storage versioning+30d lifecycle, bucket IAM) + restore runbook; (2) CI — .github/workflows/ci.yml unit+rules+e2e on PR; (3) cross-browser/device — Playwright Chromium/Firefox/WebKit + emulated Pixel7/iPhone14, real-device matrix in SMOKE_TEST §G; (4) Firestore+Storage security-rules tests, npm test now boots all 3 emulators — VALIDATED 123 integration tests pass (was 47), unit 445+185 still green; (5) axe-core a11y over public pages; (6) handoff docs RUNBOOK/ONBOARDING/KPIS/USER_TESTING/CHANGELOG. NOT deployed (owner owns deploy; backupRtdb inert until BACKUP_BUCKET). Blocked on owner inputs (handed off with precise asks): GA4 (Measurement ID + consent-banner UX sign-off) + server-side invite-open viewedAt, Clarity (project ID + PII masking), monitoring (Sentry DSN + UptimeRobot/Cloud Monitoring), backup-infra enablement (run setup-backups.sh + deploy + test restore), credential rotation + git-history scrub (destructive). Plan file: ~/.claude/plans/role-you-are-a-foamy-reddy.md.

## [2026-06-20 17:05] session | Implement + deploy the five-domain web audit
Touched: Five-Domain Audit Implementation 2026-06-20 (new), index.md, docs/{WEB-AUDIT-2026-06-20,RUNBOOK,ONBOARDING}.md, frontend (App/ConfirmationForm/InviteRsvp/PhoneInput/CityField/GlobalStyle/InviteStyles/poller/digitalPhotos/DigitalInvitationPage/services, GroomDashboard, DriverDeliveryList, Icon, Skeleton, i18n {ar,he}, index.html, public/og-default.svg, sentry.js, main.jsx, vitest + tests), backend/functions (rateLimit/keyedRateLimit, confirmations, constants/rateLimits, index, errorDetail, users/auth/guests/proofs, payments, faceIndex/purge, auditRetention, reminders, invites /digital/opened, analytics/aggregate, sentry, api/index, package.json), .github/workflows/ci.yml. Owner said "implement everything" in the audit; three decisions captured (AskUserQuestion): soften the 30-day-deletion copy (no purge job), include ALL sign-off-required work (DB/data + feature/UI + Sentry + doc fixes), commit-per-batch + I deploy. Executed in 7 batches, each build+test gated, committed specifically (never add -A — admin-SDK key in root): (1) a11y labels/landmarks/aria + 16px iOS-zoom + dead Telegram i18n + pause-on-hidden polling + ?ref hardening; (2) venue-NAT confirm rate-limit (keyedRateLimit by groomUsername) + maxInstances:20 + shared errorDetail + price-drift/i18n-parity tests + CI build+gitleaks jobs; (3) Node 20→22 (engines/firebase.json/CI); (4) invite-open viewedAt+locale endpoint + analytics open-rate + AR/HE+name reminders + audit-log retention sweep + biometric undated-wedding purge fallback; (5) onboarding checklist on handwritten-groom+driver + default OG image (SVG) + pin/warning/info icons + SkeletonList variant; (6) Sentry front+back (DSN-gated, /api/health reports monitoring); (7) softened AR/HE deletion clause to match reality + corrected the FALSE "secrets in git history" claim in RUNBOOK/ONBOARDING (verified clean: admin-SDK key + functions/.env gitignored, never committed) + RUNBOOK §9 owner activation checklist. Two brief alarms verified FALSE by hand: build healthy; admin-SDK key not in history. Final gates green: 451 FE unit / 189 BE unit / 123 integration / both builds clean. DEPLOYED to dawa-aa793: functions (all 10 on Node 22, incl. new purgeOldAuditLogs) + hosting. Smoke-tested live: /api/health {ok,monitoring:false}, POST /invites/digital/opened bad-token→400, landing serves og-default.svg+summary_large_image, demo invite renders clean (10 console errors are pre-existing demo-Unsplash CSP blocks, not regressions). Owner-gated remainder in RUNBOOK §9: WhatsApp business number (funnel inert until set), GCP+AWS budget alerts, enable backups+restore test, UptimeRobot+Sentry DSN, CI required-checks, remote gitleaks + move on-disk secrets, real testimonials, go-live secrets, og-default.png, KPI baseline. Plan file: ~/.claude/plans/role-you-are-a-shimmying-backus.md.

## [2026-06-22 17:50] session | Finish 3 deferred audit items (OG PNG, skeletons, emoji sweep)
Touched: Five-Domain Audit Implementation 2026-06-20, index.md (no change), frontend (index.html, public/og-default.png, scripts/render-og-default.cjs, components/{Skeleton,SearchBar,icons/Icon}.jsx, hooks/usePortalState.js + portal/usePortalConfirmations.js, data/status.js, pages/portal/{groom/GroomGuests,groom/GroomDashboard,groom/digital/DigitalGuests,groom/digital/DigitalDashboard,driver/DriverDeliveryList,admin/AdminUserManager,admin/AdminConfirmationsTab,admin/AdminAnalytics,admin/AdminDesigns}.jsx, pages/DigitalInvitationPage.jsx, i18n/{ar,he}.js, __tests__/data/status.test.js). Owner: "do all of this" (the 3 deferred items), answers via AskUserQuestion — emoji sweep INCL. i18n copy, skeletons on all 4 lists, OG via backend canvas renderer, OG URL = dawa.to. Shipped in 3 build+test-gated commits (hosting-only; no functions change): (A) bd1228c — render-og-default.cjs draws a branded 1200×630 card with @napi-rs/canvas + Amiri (no new frontend dep), committed og-default.png, og:image→absolute https://dawa.to/og-default.png (verified visually + 200 image/png on both origins). (B) 0497714 — added guestsLoading/confirmationsLoading portal flags (mirroring usersLoading), gated SkeletonList variant=row in GroomGuests/DriverDeliveryList/AdminUserManager/AdminConfirmationsTab. (C) 2209ddb — +21 Icon glyphs; status.js iconName (kept icon emoji for Leaflet marker); converted status/stat/triage/design badge maps + SearchBar + digital-invite error page + driver screen to <Icon>; stripped functional color emoji from all AR/HE copy via node (118 chars each, parity preserved); kept decorative dingbats ✦♛♥✎ + dead dock/process emoji. Gates: 451 FE unit / 189 BE unit / 123 integration / build clean. DEPLOYED hosting to dawa-aa793; smoke: og-default.png 200 on dawa-aa793.web.app + dawa.to, landing serves the new og:image, landing boots with 0 console errors. Remaining long-tail: a few inline location pins in secondary modals/forms (CityField/StreetField/GuestMapModal/SharedCities/DigitalYourPhotos/WalletButton/AddressInput/GroomMultiSelect/DriverMap/EditConfirmationModal/InviteForm/DigitalInviteForm) — low-risk follow-up. Plan file: ~/.claude/plans/role-you-are-a-shimmying-backus.md.

## [2026-06-25 19:40] session | Groom mobile app (Capacitor) Phase 1
Touched: [[Mobile App]], [[Architecture Decisions]], [[Payments]]

## [2026-06-26 14:05] session | Premium 3D invitation envelope (rebuilt in celestial scene)
Touched: [[Architecture Decisions]], [[Digital Invitations]]. Grilled the design plan (8 decisions: surface=digital invitation page; integration=rebuild INSIDE the shared celestial scene; backdrop=float in starfield; reveal=full ~4.8s cinematic; card content=add blessing+welcome fields; field scope=envelope card only; theme coloring=fully recolor per theme; perf gating=tier≥2). Imported the Claude Design project (`Landing.dc.html`/`envelope3d.js`) via the claude_design MCP. Implemented ONLY the envelope animation + visual effects (not the rest of the landing design, not other pages). Files: frontend `components/digital/celestial/envelopeMesh.js` (full rewrite — premium PBR cardstock + glossy wax seal w/ monogram + cream gold-foil card w/ baked AR+HE calligraphy + two gold-spark/shockwave bursts + `setOpen(t,fov,aspect)→pose` + `framePose` + dispose), `components/digital/celestial/celestialEngine.js` (renderer ACES/AA gated on `!!envelope`, particle `toneMapped:false`, telephoto `ENV_FOV=34`, ~4.8s timeline, camera-pose application + glide blend, FPS-guard suppressed during reveal), `components/digital/sections/{CelestialAmbience,CelestialEnvelopeOverlay,EnvelopeIntro}.jsx`, `components/digital/DigitalInvitationView.jsx`, `utils/themeToEnvelopePalette.js` (NEW, per-theme palette w/ contrast clamps), `data/digitalInviteDefaults.js`, `pages/portal/groom/digital/DigitalDesignEditor.jsx`; backend `functions/src/api/routes/digital/{sanitize.ts,constants.ts}` (allowlist `blessing`/`welcome`). Gates: frontend build clean, functions tsc clean, 496/500 FE unit (4 failures pre-existing in ForgotPasswordPage). Adversarial 6-dimension review (12 raw findings → 2 confirmed → both fixed: mid-reveal scroll-lock leak when wantWebGL flips false; foilBright collapsing to foil on 6 light themes). Live Playwright-MCP verify on `/d/demo/demo?demo=1` (RTX 3050 → tier 3): sealed envelope reads in starfield, tap → fracture+burst → flap → card rise w/ "كريم وليلى"+gold filigree → camera push-in → dissolve → handoff → `opened` persisted + scroll released, 0 console errors (headless rAF throttling made it LOOK stuck mid-reveal when idle — a test artifact, not a bug). Bumped key light + gold-edge opacity so the sealed envelope reads clearly. NOT committed/deployed (user didn't ask). Plan file: ~/.claude/plans/in-this-design-plan-curried-cloud.md.

## [2026-06-26 21:48] session | Groom app: Capacitor→Expo reversal + Phases 0–2
Touched: [[Mobile App]], [[Architecture-Decisions]]. Grilled the Capacitor→Expo switch (native UX made a product standard; fully-native rejected because the only justification — heavy AR/3D — is guest-facing, not the groom management app; Expo + EAS cloud iOS builds suit the Windows dev). Phase 0: extracted DOM-free @dawa/core (47 files moved to shared/src + re-export shims at old paths, env/storage adapter seams, resolved via Vite alias + Metro extraNodeModules, NO npm workspace) — FE 500/500 + BE 440/440 green (11d1f60). Phase 1: replaced the Capacitor scaffold with an Expo SDK 52 auth shell (expo-router gate, secure-store + expo-constants adapters, loadStoredTokensAsync) — Metro bundle web 655 / android 926 (41ce680). Phase 2 (grilled: faithful look + native interactions, bottom tabs + header kebab, include the media gallery): RN component library + nav shell + useGroomDigital data context + Help/Terms (b4fecd6); Manage / Guests / Dashboard (1e496e3); EAS prep eas.json + docs/mobile-device-build.md (a837bd8). Android bundle 1044 modules green; native modules datetimepicker/image-picker/file-system/sharing. Upload progress gated off (robust fetch path) pending an on-device XHR spike. Pending: EAS dev-client on a real device (needs the user's Expo + Apple Developer accounts).
