# Expo / React Native
node_modules/
# Renamed/broken node_modules left over from an install mishap (kept on disk).
.node_modules_root_broken/
.expo/
dist/
web-build/
*.log

# Native projects are generated by prebuild / EAS (managed workflow)
/ios/
/android/

# Local env
.env
.env*.local

# Misc
.DS_Store
*.orig.*
