// Native theme — re-exports the @dawa/core design tokens so RN StyleSheet code
// uses the exact same palette/spacing/type scale as the web app.
export { C, space, radius, type, shadow, z } from "@dawa/core/styles/theme.js";
